/**
 * Type of a Submission File Preview
 * @author Austen Money
 */

enum PreviewType {
    Submission = "Submission",
    AdditionalReference = "Additional Reference"
}

export default PreviewType;
