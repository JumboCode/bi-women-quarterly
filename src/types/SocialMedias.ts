/**
 * Stores strings for different social media platforms.
 *
 * @typedef {Object} SocialMedias
 * @property {string} LinkedIn - LinkedIn username.
 * @property {string} Facebook - Facebook username.
 * @property {string} Instagram - Instagram username.
 * @property {string} X - X (twitter) username.
 * @property {string} TikTok - Tiktok username.
 */

type SocialMedias = {
    LinkedIn: string;
    Facebook: string;
    Instagram: string;
    X: string;
    TikTok: string;
};

export default SocialMedias;
