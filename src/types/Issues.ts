/**
 * Publication issues available for submissions
 * @note just for testing - real issues will have actual names
 * @author Austen Money
 */

enum Issues {
    Current = "Current",
    Next = "Next",
    None = "None",
}

export default Issues;
